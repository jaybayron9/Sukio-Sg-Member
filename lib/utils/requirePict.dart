import 'package:flutter/material.dart';

class RequirePict extends StatefulWidget {
  const RequirePict({ Key? key }) : super(key: key);

  @override
  _RequirePictState createState() => _RequirePictState();
}

class _RequirePictState extends State<RequirePict> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}